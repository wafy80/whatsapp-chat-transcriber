# =====================================================================
# WhatsApp Chat to PDF Transcriber - Configuration File
# =====================================================================
# Copy this file to config.ini and customize as needed
# All measurements in inches unless otherwise specified
# =====================================================================

[PDF]
# Page size: A4 or letter
page_size = A4

# Margins (in inches)
left_margin = 0.5
right_margin = 0.5
top_margin = 0.5
bottom_margin = 0.5

# Maximum image dimensions (in inches)
max_image_width = 3.0
max_image_height = 2.5

# Page break after N messages (0 = no page breaks)
page_break_after_messages = 0

[WHISPER]
# Whisper AI model size
# Options: tiny, base, small, medium, large
# Larger models are more accurate but slower and require more RAM
# Recommended: small (good balance of speed and accuracy)
model = small

# Language for transcription
# Examples: en, es, fr, de, it, pt, ru, ja, zh, ar, hi, ko
# Leave empty for auto-detection (slower but works for any language)
language = 

[LANGUAGE]
# =====================================================================
# TRANSLATION SYSTEM (DEFAULT)
# =====================================================================
# Interface strings are loaded from languages/XX.ini files
# 
# These control:
# - WhatsApp export patterns ("file attached" vs "archivo adjunto")
# - PDF labels ("Audio:", "IMAGE", etc.)
# - System messages
# =====================================================================

# LANGUAGE CODE (ISO 639-1)
# Examples: en, es, fr, de, it, pt
# The program will automatically load languages/XX.ini
# Leave empty to use Whisper's detected language
code = en

[COLORS]
# Colors in hexadecimal format (without # prefix)
# WhatsApp green: 25D366
# WhatsApp teal: 075E54
# WhatsApp blue: 0084FF

title_color = 075E54
sender_color = 25D366
media_color = 0084FF
system_color = 808080

[PRIVACY]
# Exclude images from PDF (show filename only)
# Useful for sharing transcripts without personal photos
exclude_images = false

[HTML_TEMPLATE]
# =====================================================================
# HTML TEMPLATE SYSTEM (DEFAULT)
# =====================================================================
# PDFs are generated from HTML templates for maximum flexibility
# =====================================================================

# ENABLE HTML TEMPLATES (enabled by default)
enabled = true

# HTML TEMPLATE FILE
# File must be in the templates/ directory
# 
# Available templates:
#   templates/template.html         - Full WhatsApp-style layout with statistics (default)
#   templates/template_minimal.html - Minimal essential layout
#   templates/template_simple.html  - Simple clean layout
#
# You can create your own custom templates!
template_file = templates/template.html

# USER NAME
# Used to distinguish user's messages from others
# User messages will have class="user", others class="other"
# Leave empty for automatic detection from filename
user_name =

# SHOW STATISTICS
# If true, shows statistics in header (messages, media, transcriptions)
show_stats = true

# CUSTOM FOOTER TEXT
# Additional text to show in document footer
# Leave empty for default
footer_text = Generated with WhatsApp Chat to PDF Transcriber

# =====================================================================
# ADVANCED LAYOUT SETTINGS (for ReportLab fallback mode)
# =====================================================================
# These settings are only used if HTML templates are disabled
# Most users won't need to modify these
# =====================================================================

[LAYOUT]
# Font sizes (in points)
title_font_size = 20
sender_font_size = 10
message_font_size = 9
time_font_size = 8
system_font_size = 8
media_font_size = 9

# Font families
title_font = Helvetica-Bold
sender_font = Helvetica-Bold
message_font = Helvetica
time_font = Helvetica
system_font = Helvetica-Oblique
media_font = Helvetica

# Spacing after elements (in points)
title_space_after = 20
sender_space_after = 4
message_space_after = 8
time_space_after = 3
system_space_after = 8

# Left indentation (in points)
message_left_indent = 0
time_left_indent = 0

# Spacers (in points)
spacer_title = 10
spacer_subtitle = 20
spacer_before_image = 6
spacer_after_image = 6
spacer_between_messages = 10

# Text alignment: LEFT, CENTER, RIGHT, JUSTIFY
title_alignment = CENTER
message_alignment = JUSTIFY

# =====================================================================
# LEGACY TEMPLATE SYSTEM (deprecated - use HTML templates instead)
# =====================================================================
# This section exists for backward compatibility only
# For new projects, use HTML_TEMPLATE section above
# =====================================================================

[TEMPLATE]
enabled = false
html_enabled = false 
